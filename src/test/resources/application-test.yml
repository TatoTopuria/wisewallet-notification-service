spring:
  cloud:
    aws:
      credentials:
        instance-profile: false
      secretsmanager:
        enabled: false
  mail:
    host: localhost
    port: 1025
    properties:
      mail:
        smtp:
          auth: false

wisewallet:
  internal:
    jwt:
      secret: default-internal-jwt-secret-min-32-chars-for-test!
    api-key: default-internal-api-key

